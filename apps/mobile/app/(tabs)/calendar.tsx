import { SafeAreaView, Text } from "react-native";

export default function CalendarScreen() {
  return (
    <SafeAreaView>
      <Text className="text-white">Calendário</Text>
    </SafeAreaView>
  );
}
